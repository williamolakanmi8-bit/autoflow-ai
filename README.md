# autoflow-ai
Built a multi-agent automation system that picks up business triggers, uses LLMs to figure out what needs to happen, then sends the right agent to handle it — whether that's sending outreach emails, updating the CRM, or posting Slack alerts. Stack includes Python, LangGraph, OpenAI GPT-4o, FastAPI and HubSpot API.
